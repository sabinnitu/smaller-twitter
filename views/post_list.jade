extends layout

block content
    h1=title

    ul
        each post in post_list
            li
                a(href=post.url) #{post.message}
                i  added by #{post.user.name}

        else
            li There are no posts.

    a.btn.btn-sm.btn-default(href='/') Back